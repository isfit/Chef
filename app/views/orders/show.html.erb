<p class="btn-group span3 pull-right" >
  <%= link_to edit_order_path(@order), class:"btn" do %>
    <i class="glyphicon glyphicon-pencil"></i> Rediger
  <%end%>
  <%= link_to @order, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn" do %>
    <i class="glyphicon glyphicon-trash"></i> Slett
  <% end %>
</p>
</p>
<h1>Ordre# <%= @order.id %></h1>
<table class="table span11">
  <tr>
    <th>Leveringsadresse</th>
    <th>Leveringstidspunkt</th>
  </tr>
  <tr>
    <td>
      <h3><%= link_to @order.user.given_name + " " + @order.user.family_name, "http://internal.isfit.org/users/#{@order.user.id}" %></h3>
      <h3><%= simple_format(@order.location.name) %></h3>
      <h3><%= simple_format(@order.location.address) %></h3>
      <h3><%= simple_format(@order.deliver_to) %></h3>
    </td>
    <td>
      <h2><%= @order.delivered_at.strftime("%a, %d. %b") %></br>
        <%= @order.delivered_at.strftime("%H:%M") %></h2>
    </td>
  </tr>
</table>

<table class="table span11">
  <tr>
    <th>Totalt antall</th>
    <th><%= @order.total_order_amount %></th>
  </tr>
  <tr>
    <th>Måltid:</th>
    <th>Mengde:</th>
  </tr>

  <% @order.meals.each do |meal| %>
    <tr>
      <td><%= meal.meal_type.title %></td>
      <td><%= meal.amount %></td>
      <tr>
      <% end %>
    </table>
